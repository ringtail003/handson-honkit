{
  "name": "honkit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "honkit": "honkit",
    "honkit:serve": "npm run honkit serve",
    "honkit:build": "npm run tailwind:build && npm run honkit build ./ --log=debug --debug",
    "tailwind": "tailwindcss",
    "tailwind:build": "npm run tailwind -- --input=./docs/styles.css --output=./docs/dist.css",
    "tailwind:serve": "npm run tailwind:build -- --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "honkit": "^4.0.0",
    "tailwindcss": "^3.1.8"
  }
}
